#
# (C) 2011-2013 KATO Takeshi
#


def configure(x):
	if x.env.MULTIBOOT or x.env.MULTIBOOT2:
		x.recurse('multiboot')

def build(x):
	if x.env.boot_fd:
		x.recurse('fd')

	if x.env.MULTIBOOT or x.env.MULTIBOOT2:
		x.recurse('multiboot')
